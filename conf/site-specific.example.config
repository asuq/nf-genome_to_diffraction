// Copy this file outside Git and adapt it to the target HPC site.
// Do not add credentials, licence data, or machine-specific private paths here.

process.executor = 'slurm'

process {
    // queue = 'site_queue'
    // clusterOptions = '--account=site_account --export=NONE'
}
