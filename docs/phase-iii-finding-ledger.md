# Phase III finding ledger

Status: active Phase III ledger after immutable experimental release `v0.2.0`.
Update a row only with a named regression and immutable evidence pointer.
`Fixed` means the source contains the correction; it does not by itself
establish the later Phase III release gate.

## Original 2026-08-17 review

Baseline disposition: 12 fixed, one superseded/deleted, five partial, and 16
open among the original 34 findings.

| Finding | Baseline disposition | Phase III gate / required evidence |
| --- | --- | --- |
| `PIPE-P1-01` | Partial; identity, composition consumption, and local crystal isolation fixed | Raw FAA/annotation/MTZ, database inventory, source commit/tree, nf-helper, Pixi lock, execution policy, seven Phenix executables, and adapter versions form one path-free content identity carried by every selected composition task. An observed crystal-local candidate mutation reruns exactly one affected task while preserving 33 sibling/shared cache identities and output bytes. Independent raw/tool mutation evidence and other task-family consumption remain open. |
| `PIPE-P1-02` | Fixed locally; real multi-item qualification pending | The production first-copy, approved-seed, and refinement workflows convert each shared singleton catalogue/model/review/diffraction input to a reusable value instead of allowing the first sibling to consume it. A real local Nextflow regression reproduces the former one-hypothesis/one-seed/one-finalist truncation, then requires exactly three hypothesis tasks, two seed tasks, and two finalist tasks with unchanged identities on a fully cached resume. The separate three-crystal unknown stub and 25-attempt/three-parent composition fan-out remain independently qualified; real Phenix/scheduler execution is pending. |
| `PIPE-P1-03` | Fixed locally; real provider qualification pending | The fixed local graph composes one enabled no-hit, two configured-disabled routes, one unsupported route, and a typed all-routes `completed_no_model` registry through cached Nextflow without a network-labelled process. The ordinary application now carries complete typed PDB/Foldseek/AFDB result inventories into zero-coordinate and zero-model preparation, preserves mixed populated/empty providers, publishes the complete no-eligible-model registry, and builds an honest zero-candidate MR-seed checkpoint. Missing upstream evidence, truncated catalogue coverage, contradictory results, or missing registration checksums fail closed. Real provider/HPC qualification remains pending. |
| `PIPE-P1-04` | Partial; in-job denial fixed locally; login acquisition pending | Direct PDB coordinate registration, exact AFDB retrieval, and M6 coordinate staging all declare `process_network` plus both reviewed site aliases: Marmic `run_local` and Viper `needs_internet`. Those aliases keep bounded tasks on the outer Nextflow controller but do not grant network, because the controller itself runs inside Slurm. Every child and controller-local Nextflow task now enters the fixed fail-closed user/network namespace shell. M6 trace/resource collection retains its controller-stage record separately from native Slurm children. Selected-site socket denial, complete pre-submit provider staging, and real provider execution evidence remain pending. |
| `PIPE-P1-05` | Fixed | Dataset-qualified duplicate-label tests reject conflicts and retain deterministic equivalent selection. |
| `PIPE-P1-06` | Partial; production first-copy/additional-copy/refinement propagation fixed locally | Schema-v2 binds a dataset-qualified MTZ selection, every override source, and selection-derived Phaser/refinement command identities. Production first-copy and approved same-component searches now independently verify the exact crystal, hypothesis, preflight, observations, and source MTZ; both emit explicit installed-runtime-qualified space-group and low/high-resolution parameters. Reviewed refinement receives one complete crystal-bound selected dataset, Free-R identity, source MTZ, preflight, finalist, catalogue, and Phenix manifest. Before execution, the raw MTZ and derived parent must match in selected observation values and source HKL-to-Free-R membership. A selection mutation reruns exactly two approved placement tasks and two refinement finalists while seven unrelated historical jobs stay cached; rejected/deferred states launch no placement. Legacy v1 commands and identities remain unchanged. Real Phase III execution remains pending; symmetry/reindexing equivalence is not claimed. |
| `PIPE-P1-07` | Partial; production raw identity and parent/child preservation fixed locally | Dataset-qualified labels, finite integral non-constant distributions, and permutation-invariant raw HKL-to-flag digests are covered by `test_free_r_identity_v2.py`. Production Phase III crystal dispatch now publishes the exact raw Free-R identity beside its selected observations and refuses missing, constant, or cross-dataset arrays before output publication; no test convention is invented. Every reviewed production refinement process now receives that exact crystal-bound identity plus its source MTZ, preflight, and selected observations in one deep-content-cached item. Phase III brief refinement rejects a changed/missing parent Free-R mapping before tool invocation, selects the exact label, requires existing flags, disables generation, preserves explicit or unresolved test-value semantics, and separately blocks promotion on missing/changed child flags. A valid derived parent and row-permuted refined child remain accepted. Qualification against a real Phenix-derived MTZ remains open. |
| `PIPE-P1-08` | Fixed locally and public-control qualified; general-attempt HPC pending | Fixed-component searches retain each reviewed model identity/error source. Marmic control `634631` accepted exact 6RTZ/3U7Q component partitions, and control `634860` accepted the installed multi-fixed A+B/search-C syntax with packed 9ECN `2A+2B+2C`, exact per-component coordinates, and 18,842-atom recombination. The general one-attempt executor now resolves those component-only coordinates and original uncertainties without collapsing the parent, independently validates its registry/execution/MTZ/Free-R authorities, and emits only claim-free child-state evidence. One fixed general-attempt HPC qualification remains. |
| `PIPE-P1-09` | Fixed on `dev/phase3`; integration evidence pending | T12 accepts only a new or empty attempt-owned directory; stale files fail before tool execution and cannot be published. |
| `PIPE-P1-10` | Fixed | Duplicate JSON/YAML keys fail through every loader. |
| `PIPE-P1-11` | Fixed | Strict wire types and non-finite rejection cover every runtime loader. |
| `PIPE-P1-12` | Fixed | Duplicate Matthews/preflight/group/source identities fail. |
| `PIPE-P2-01` | Fixed on `dev/phase3`; integration evidence pending | Zero-exit refinement without parsed final Rwork/Rfree becomes `failed_parse`; completed result contracts require both final values. |
| `PIPE-P2-02` | Fixed on `dev/phase3`; integration evidence pending | Unknown or inconsistent sequence-map catalogue identities emit candidate-level `failed_parse` records instead of aborting sibling finalists. |
| `PIPE-P2-03` | Fixed | Runtime, tracked, and packaged schemas are byte/semantic parity checked. |
| `PIPE-P2-04` | Fixed | Duplicate headers and ragged TSV rows produce typed diagnostics. |
| `PIPE-P2-05` | Fixed | All executed Phenix binaries are digest-bound and replacement is refused. |
| `PIPE-P2-06` | Partial; reviewed crystal/hypothesis/A-seed/finalist/refinement/composition/sequence canonical-entry fan-out fixed locally | Explicit Phase III first-copy application replaces the legacy single-crystal selector with one manifest-owned dispatch, joint-A funnel, complete hypothesis tasks, and an independent review package per proceeding crystal; one catalogue/provider preparation remains shared. Existing registry-owned crystallographic decisions and frozen MTZs are revalidated before scheduling; a local Nextflow regression retains one held crystal and runs only its two proceeding siblings. Production `phase3_application.nf` resolves each approved A package exclusively from its completed-screen registry, independently revalidates full parent/execution/review ownership, and joins complete placement/finalist inputs only by crystal ID. Every refinement, complete-catalogue sequence checkpoint, and independently owned sequence/composition review package retains its exact selected diffraction, Free-R, catalogue, source, parent, command, maps, coordinates, checksum inventory, and runtime evidence. Only the current single-component scheduler run can own its exact sequence-group or successfully refined-state targets; fixed operator handoffs independently resolve each package by owned run/checkpoint and infer the crystal from checksum-confirmed review rows. Cross-crystal directory, command, preflight, identity, predecessor-run, invented locus/state, or evidence mutations fail. A real three-crystal canonical-entry regression schedules three review stages, one required placement, two finalist stages, two refinements, two sequence checkpoints, two owned sequence packages, and two owned composition packages; an already-complete A state needs no fabricated addition and a deferred crystal launches no science. All tasks cache, and revising one review reruns only that crystal's approval, placement, finalist stage, checkpoint, and two packages while unchanged refinement and sibling tasks remain cached. Two-catalogue cardinality, malformed-sibling isolation, real Phenix execution, and a fixed unknown profile remain open. |
| `PIPE-P2-07` | Fixed locally; real scheduler qualification pending | Only an explicitly classified temporary infrastructure failure exits with `EX_TEMPFAIL` 75; transient HTTP 408/425/429/5xx and exhausted transport timeouts are classified, while permanent HTTP, input contracts, parsers, and scientific failures keep their non-retryable exit. Nextflow retries exit 75 once and preserves `finish` for deterministic candidate-level failures. A real local Nextflow fixture records transient attempts one/two and one deterministic attempt; focused CLI/provider/policy tests reject retry-class leakage. |
| `PIPE-P2-08` | Fixed | Declaration-only toggles/caps are removed; every retained cap has one runtime consumer. |
| `PIPE-P3-01` | Fixed locally and offline build/install-qualified; clean-break exact-source CI pending | `hatchling==1.32.0` remains exact in build metadata and `pixi.lock`. The installed research wheel now exposes only `genome-to-diffraction` and explicitly excludes the repository-internal `genome_to_diffraction/hpc/` client. Source-checkout operators retain that client through the named Pixi task and separately built immutable zipapp. The locked offline wheel gate passes with one public entry point, ten packaged schemas, installed version `0.2.0`, and no internal HPC files; missing-entry-point and internal-client inclusion regressions fail closed. The earlier two-entry-point gate on `1fd2a378309bb4eaeba0c2a6b0a36f6f02294ef4` remains historical evidence, not the current package contract. |
| `DEV-P0-01` | Fixed | Frozen family snapshots and private truth classify retained PDB/entity attempts. |
| `DEV-P0-02` | Fixed | Typed identity decisions make wrong open-set reports hold. |
| `DEV-P0-03` | Fixed | Edge outcomes derive from observed evidence, not descriptors. |
| `DEV-P1-01` | Fixed | Every catalogue receives complete shared batch/result values. |
| `DEV-P1-02` | Fixed locally; M6 rerun pending | Ordinary preparation whitelists only HKL, the deterministic dataset-qualified observation/sigma array(s), and one finite integral non-constant Free-R array from the same dataset. It verifies exact HKL and HKL-to-flag membership before/after writing; the runner builder requires and independently validates a path-free output-only record but excludes it from the runner archive. Original structure-factor provenance remains outside the runner. FWT/PHWT/FC/PHIC mutation leaves sanitised bytes and runner cache identity unchanged; missing/ambiguous arrays fail closed. Frozen map-only edge cases remain explicit exceptions. |
| `DEV-P1-03` | Fixed locally; M6 integration evidence pending | Shared truthless discovery retains a fixed 25-hit/query/route envelope and binds the changed search/policy adapter identities. Operational cases recover the historical first-three effective input. Leakage cases apply only checksum-bound runner-visible amino-acid identity/coverage evidence before the unchanged three-hit accepted-model cap. A seven-ranked-hit-per-route regression retains the safe fourth through sixth direct and Foldseek hits, deterministically annotates each seventh as cap-deferred, produces byte-identical scientific outputs under row permutation, and emits typed `completed_no_model` output when all hits are excluded. The protocol and thresholds are unchanged; no private target/family truth was added to runner inputs. |
| `DEV-P1-04` | Fixed locally; M6 rerun pending | One login/controller-labelled task resolves each case's bounded selected PDB hits through the existing qualified cache, materialises checksum-addressed local objects, and emits relative path-free source records. Active and early case workers no longer receive a database manifest or call coordinate registration; no-hit and missing-model paths remain typed. Registration reuse, local relative-object consumption, network-authority exclusion, and cached stub fan-out have focused regressions. |
| `DEV-P1-05` | Superseded/deleted | Unsafe shared-store consumer path was removed in favour of standard Nextflow resume. |
| `DEV-P1-06` | Fixed locally; M6 integration evidence pending | Seed adapter v2 retains every packing-eligible copy hypothesis in one deterministic selected/deferred inventory and applies the unchanged five-seed cap only after LLG, TFZ, and candidate-rank ordering. Copy count is neither a replacement rule nor a ranking key; immutable hypothesis ID is the final tie-break. A same-model two-copy strong/four-copy weak regression keeps the two-copy seed selected, retains the four-copy alternative as cap-deferred, and produces identical bytes across all input permutations. |
| `DEV-P1-07` | Fixed locally; real M6 qualification pending | The full 26-task stub mutates the real protocol input and observes exactly ten dependent tasks, including coordinate staging, complete while 16 unaffected tasks remain cached with byte-identical child outputs. Production child evidence now distinguishes a fresh operational baseline from the leakage baseline: only truthless import/search tasks may be cached for leakage, every track-specific task must complete, and every resume task must cache. Final collection rehashes the operational parent precheck and requires the reused task hashes and complete non-staged child-file inventories to match that parent; missing, changed, unexpected, or mismatched children fail closed. Deleting one cached catalogue `source_records.jsonl` leaves all 26 Nextflow tasks cached but makes the production verifier fail. The existing `cache_invalidation_verified` field proves content-key sensitivity only; real M6 execution and a checksum-bound observed cache-key mutation record remain separate acceptance gates. |
| `DEV-P2-01` | Fixed locally; M6 integration evidence pending | The active M6 graph now orders all three `groupTuple` child inventories by immutable hypothesis or seed ID before constructing downstream process inputs/cache identities. Discovery already sorted validated batch IDs and then result/hit IDs; policy outputs already sorted retained/rejected/ranked model inventories; seed selection already applied its evidence rank plus immutable hypothesis tie-break; finalists and the final track already used stable seed and frozen case-ID order. Case evidence now rejects duplicate refinement children and sorts the otherwise unordered refinement/sequence child pairs by seed ID while preserving ranking-bearing seed rows and sequential copy-series order. Reversed provider-batch inputs produce identical validated result/hit/manifest inventories, and reversed complete refinement-child inputs produce byte-identical case-evidence trees; the existing six-candidate seed regression covers all 720 permutations, and the leakage-policy regression proves byte-identical outputs for reversed direct/Foldseek model rows. No adapter version changed because only unordered completion serialisation changed; scientific rank/order semantics are unchanged, and canonical child argument order changes the relevant Nextflow cache identity exactly when an older completion order differed. |
| `DEV-P2-02` | Fixed locally; real profile qualification pending | M6 operational/leakage, archived M4 copy, and T12 scientific wrappers create an isolated `cache/apptainer` directory inside the exact owned run and export that path to Nextflow. The wrapper contains no hard-coded user/account cache; the existing M6 smoke retains its independently validated run-owned cache. Focused contract regressions cover all three scientific functions and Bash syntax; real-site scientific acceptance remains a separate gate. |
| `DEV-P2-03` | Fixed locally; integration evidence pending | The retired direct control/M6 benchmark actions, their fail-only execution functions, obsolete request types, and compatibility-only tests are deleted. Production helper modules expose no scientific driver or thread/process-pool primitive; shared preparation/classification helpers used by the owned Nextflow graphs and immutable historical evidence remain intact. Focused parser rejection and repository policy forbid reintroducing a nested execution authority. |

## Findings added after the original review

| Finding | Baseline disposition | Phase III gate / required evidence |
| --- | --- | --- |
| `PH3-P0-01` P6 composition claims self-asserted | Fixed and accepted in `v0.2.0` | Missing/wrong B and 9ECN consume typed assessments; packed wrong B remains search evidence only. |
| `PH3-P0-02` P6 control identities under-bound | Fixed and accepted in `v0.2.0` | Exact parents, MTZ/models, source preparations, catalogue-minus-A universe, and all 47 checksums match. |
| `PH3-P0-03` Empty partner channel unexecuted | Fixed and accepted in `v0.2.0` | Exact-source CI schedules zero partner tasks, one summary, and byte-identical cached resume. |
| `PH3-P1-01` Control-only heteromer bridge | Fixed locally; real application qualification pending | A normal `analysis_stage=heteromer` run consumes its own reviewed approved A seed and catalogue-derived partner plan without requiring 6RTZ preparation or scheduling the fixed-control process. Supplying the optional explicit control preparation preserves the historical 6RTZ process alongside the same catalogue search. A focused actual Nextflow stub verifies both process inventories, checkpoint preservation, control-only output isolation, and normal cached resume. |
| `PH3-P1-02` B registry restricted by A cap | Fixed on `dev/phase3`; real execution pending | Complete candidate generation selects from the checksum-verified all-model registry without accepting an A shortlist; a valid B model outside the 25-model A set is retained and schedulable. Every parent/candidate-copy model resolves through the registry or retains a typed unsearchable reason; cap mutations leave the registry unchanged, while registry/source mutations change the plan identity. |
| `PH3-P1-03` Parent uncertainty dropped | Fixed on `dev/phase3`; real qualification pending | The reviewed parent command supplies its original identity/error source to the fixed ensemble, result, command, and cache identity; a 35%-identity regression remains 35%. |
| `PH3-P1-04` Credible status lacks final evidence/crystal binding | Fixed locally; live workflow wiring pending | The content-addressed pass-1 assessment derives promotion only from one matching crystal/state, supported equal requested/observed copies, packing, completed refinement, parsed final Rwork/Rfree, and exact crystallographic/A-seed/composition package-decision evidence. The local collector independently re-derives every assessment ID/status and requires every referenced digest in that crystal's checksum allow-list before publication. Missing, ambiguous, mutated, or mismatched evidence fails or derives the existing typed uncertain boundary; focused tests cover each boundary. |
| `PH3-P1-05` General component depth unvalidated | Fixed and Marmic-qualified at depth three | Schema-v2 enforces provisional depths above three. Marmic control `634631` checksum-retains exact 6RTZ/3U7Q component evidence. After two fail-closed diagnostic iterations, exact-source successor `634860` recovered frozen 9ECN `2A+2B+2C` with packed component-specific A/B/C evidence, exact 18,842-atom recombination, and all 46 checksums. The generic adapter remains claim-free; depths four through six remain provisional and wrong-C/no-false-C validation remains separate. |
| `PH3-P1-06` Selected crystal status promoted globally | Fixed locally; live execution pending | Each terminal assessment is content-bound to one crystal and owned execution. The local three-crystal collector embeds all three canonical assessments, preserves each mixed status in JSONL/manifests/HTML, and deliberately has no panel-wide scientific status, so sibling success cannot promote a no-hit, held, or failed crystal. |
| `PH3-P1-07` Provider empty channels untested end to end | Fixed locally; real provider qualification pending | One authoritative mixed plan drives an enabled no-hit, configured-disabled, unsupported/provider-unavailable, and all-routes no-model terminal through six exact Nextflow tasks; every query/result is retained, resume is byte-identical, and non-stub/network execution fails closed. Ordinary application wiring also carries complete typed result inventories through actual registration, predicted/experimental preparation, mixed/all-empty first-copy ranking, and a zero-candidate review package without starting downloads, Phenix model processing, or Phaser. Unexplained empty files remain input failures. |
| `PH3-P1-08` Unknown-panel mixed outcomes cannot finalise | Fixed locally; live execution pending | The checksum-closed local collector finalises one credible solution, one scientific no-hit, and one parser failure as three retained terminal endpoints and also retains shortlist-only, crystallographic hold, and insufficient-evidence mixtures. Commands/results/evidence, per-crystal manifests, a cross manifest, and a no-promotion HTML table are byte-identical under input permutation. |
| `PH3-P1-09` Component-specific scores obscured by parent | Fixed locally; live Phaser parsing pending | One content-addressed expansion score record separately retains the exact parent combined LLG, new combined LLG, candidate-ensemble TFZ, and mathematically verified incremental LLG. It requires complete exact-model parent/candidate placement groups, the candidate's own ensemble, result-record checksum, requested/observed copies, and candidate-only coordinates. Packed candidate placements remain unresolved search evidence, never sequence/composition claims. Focused 2A+2B+2C mutations reject parent-ensemble TFZ, combined-as-incremental LLG, swapped crystal/model/result, and self-asserted identity support. |
| `PH3-P1-10` Deeper component claims lack validation boundary | Fixed; depth three Marmic-qualified | Schema-v2 evaluates unvalidated component depth before budget exhaustion or reviewer hold. Every completed retained state at depths four, five, and six remains `provisional_unvalidated_component_depth` even with packing, reviewed refinement, apparent composition support, no residual content, and a final decision checksum; complete-composition claims remain impossible. Stop reasons and review evidence stay explicit. Six mutation regressions cover all three depths. The generic A+B/search-C result remains hard claim-free, while exact-source 9ECN job `634860` applies truth only in its separate final known-control assessment and validates positive depth three. |
| `PH3-P1-11` Per-parent plans multiply the depth budget | Fixed on `dev/phase3`; live execution pending | One parent-bound depth plan and its content-addressed stub execution inventory prove exactly one maximum of 25 attempts across the complete three-parent beam, a 100-attempt global bound, exact task IDs/counts, typed no-model empty scheduling, and byte-identical cached resume. |
| `PH3-P1-12` Unverified DeepTMHMM image command could be guessed | Fixed contract; runtime integration blocked | User image/input checksums are bound, but command and parser remain empty with `blocked_unverified_cli` until the supplied image is inspected. |
| `PH3-P1-13` Conflicting localisation could become a hard exclusion | Fixed locally; real runtime qualification pending | Catalogue-wide policy and candidate generation map conflicting, unknown, and failed observations to retained first-wave-eligible `neutral`; only explicit membrane/surface/extracellular/transmembrane calls are first-wave excluded, every exclusion remains a complete planner row, and reopen changes only wave eligibility after the exact complete zero-pack trigger. |
| `PH3-P1-14` Review decisions could bind a hand-written, under-specified package | Fixed locally; real HPC collection/execution pending | All four crystallographic, A-seed, composition, and sequence packages content-bind the parent, execution identity, crystal, targets, checkpoint-specific worksheet, and complete evidence checksum/size allow-list. A single-run local registry resolves only fully validated packages by exact run/crystal/checkpoint; the existing stager preserves reviewed partial/alternative decisions without promoting a claim. The unknown bridge publishes an independently revalidated three-crystal index, and the actual Phase III application verifies that index, frozen MTZs, execution identity, canonical decision files, and exact manifest membership before routing proceeding crystals; held siblings cannot schedule Phaser. The fixed local A-seed bridge additionally authenticates one ASCII operator TSV and confirmation SHA against its exact owned `unknown-screen` parent and registry-resolved package. Production `phase3_application.nf` independently resolves and authenticates each crystal's completed-screen registry, full execution identity, exact registered A package, human stage, and complete historical MR evidence before placement/finalist/refinement. Screen-generated A packages may predate completion without permitting stale crystallographic packages. Deferred decisions launch neither placement nor refinement, while an already-complete approved state proceeds without an unnecessary Phaser job. A real three-crystal canonical-entry regression proves fully cached replay and crystal-local decision invalidation. The original v1 path remains unchanged. Fixed remote collection and real HPC qualification remain pending. |
| `PH3-P1-15` Phase III control failures discarded application diagnostics | Fixed locally; real failure classification pending | Existing `heteromer-smoke` and `phase3-phenix-probe` logs are now in the reviewed diagnostic allow-list, and their exact owned run IDs are normalised before fingerprinting. Focused regressions prove the same error remains stable across run IDs/timestamps/source digests/Slurm IDs while distinct root causes produce distinct signatures; no external operation or running job changed. |
| `PH3-P1-16` Login staging was counted as a Slurm child | Fixed locally; M6 execution pending | The fixed M6 smoke now requires exactly 26 complete tasks: 25 native Slurm children plus one separately retained, driver-bounded coordinate controller stage. Operational resume caches all 26, leakage reuses only the six truthless discovery children, and all 20 track-specific tasks including coordinate staging execute again. Historical resource records without controller stages remain readable, while production M6 collection reconciles its complete resume inventory against scheduler children plus controller stages. |
| `PH3-P1-17` Scientific M6 execution assumed Viper and main | Fixed locally; real M6 qualification pending | The fixed scientific controller accepts only the reviewed Marmic/Viper sites and literal `dev/phase3`/`main` source ancestry. Marmic staging independently binds the already-qualified Phenix manifest/checksum; both sites bind their exact Nextflow profile, frozen execution policy, and run-owned Apptainer cache before scheduler submission. Collection rejects mixed-site tracks, evaluation verifies the matching frozen policy hash, and historical Viper evidence remains readable. Focused fake-site staging/submission and wrong-site/policy regressions pass; no real scientific run has been launched. |
| `PH3-P1-18` M6 runner staging assumed a usable Git mirror | Fixed locally; real M6 qualification pending | For only the two classified absent/invalid-mirror failures, the scientific controller streams the existing bounded, exact-commit, pinned-helper source archive followed by the separately confirmed truth-isolated runner archive. The dispatcher validates both independent checksums/sizes and all 63 cases before completing site-bound staging. Focused controller tests cover both failure classes, and a fake Marmic lifecycle proves complete staging with its bare mirror absent; no scientific job was launched. |
| `PH3-P1-19` Full-panel inventory poisoned independent crystal caches | Fixed locally; live application qualification pending | Every complete crystal/A task retains its own exact item, MTZ/model, review stage, shared preparations, and immutable execution identity; the full three-crystal inventory remains separately retained outside child inputs. A real local Nextflow mutation changes one crystal's typed no-model candidate, reruns exactly that crystal, and preserves 33 unaffected task hashes plus byte-identical sibling outputs; before the fix, the same mutation reran 28 tasks including all 25 unrelated A attempts. |
| `PH3-P1-20` Joint A searches silently downgraded to one copy | Fixed locally; real joint Phaser qualification pending | An explicit Phase III diverse-funnel mode retains the configured four physically possible copy alternatives, excludes copies above four before per-model truncation, searches every selected hypothesis's complete declared copy count jointly, and enforces one 25-attempt A budget. The legacy funnel and historical output identities remain unchanged without opt-in. Every schema-v2 unknown A task now carries its exact one-to-four requested copy count; a mismatch against its selected hypothesis is rejected before fan-out. |
| `PH3-P1-21` Full-catalogue Foldseek search stopped at its pilot cap | Fixed locally; real complete-catalogue execution pending | Explicit Phase III application sorts all sequence groups into immutable Nextflow-owned batches of at most 128, invokes the existing provider-bound adapter once per batch with one high-memory job at a time, and independently verifies complete result/hit/raw-log coverage before merging. A 1,621-group public-only regression schedules exactly 13 searches and preserves all task identities/output bytes on cached resume. Missing, duplicated, deferred, or changed batches fail closed; direct PDB and historical single-batch modes remain unchanged. Real installed-tool and fixed-HPC qualification remain pending. |
| `PH3-P1-22` Empty A-seed outcomes could not receive Phase III review packages | Fixed locally; real mixed-panel qualification pending | The production multi-crystal first-copy workflow now emits one authenticated, owned schema-v2 A-seed package per proceeding crystal. Its existing legacy review/solution identities, original crystal-bound hypotheses, complete result assets, review-output checksums, candidate/inspectable counts, full execution identity, and exact scheduler-owned parent are independently verified before publication. A scientifically completed zero-candidate crystal receives the same content-bound package with an honest header-only target worksheet; a held crystal never schedules an A package. Every other checkpoint and historical v1 package still requires review targets; missing, contradictory, edited, cross-crystal, or failed evidence is rejected before publication. |
| `PH3-P1-23` Partial 9ECN failure evidence was not collectable | Fixed and remotely exercised | Marmic successor `634831` executed the corrected joint-A Phaser command but stopped at `failed_parse` before the final 9ECN checksum manifest. The collector consequently omitted the normalised result, raw log, command, capture, preflight, and optional solution assets needed to distinguish a scientific no-solution from a parser defect. CI-qualified commit `af6a751` admits only those explicit run-owned partial paths when the final checksum manifest is absent; successful checksum-driven collection is unchanged. Recollection of the same failed run retained all available parent evidence without a scheduler action and preserved its failure signature. |
| `PH3-P1-24` Valid tNCS terminal annotation and packing grammar were rejected | Fixed and Marmic-qualified | Recollected run `634831` proved Phaser wrote a packed two-copy A PDB/MTZ with LLG 3507.703 and TFZ 19.3. The parser required `Solution #1 annotation` although the valid log used `Solution annotation`, and required plural packing grammar although the final record was `1 packs of 1 accepted solution`; recognising only TFZ would have retained an earlier 13/13 intermediate count. The narrow optional-`#1` and singular/plural correction replays the raw log as accepted/packed 1/1. Exact-source successor `634860` then parsed that form and completed the entire A+B+C control successfully. |
| `PH3-P1-25` Live unknown route omitted localisation and gel evidence | Fixed and exact-source CI-qualified; application HPC pending | `provider_discovery` and `first_copy` now require one complete PSORTb/DeepTMHMM/gel bundle, independently validate it before shared preparation, bind exact sequence-group and source-record bytes, and carry per-group evidence into A hypothesis/cache identities. The empty gel manifest remains neutral. First-wave exclusions are retained as typed skipped hypotheses rather than discarded. The real network-none bundle validates all 1,621 groups as 1,310 active, 88 neutral, and 223 excluded; the complete locked gate and exact-source CI `33159922527` pass. |
| `PH3-P1-26` Localisation execution provenance was self-asserted | Fixed and exact-source CI-qualified | The raw importer no longer accepts caller-authored engine/network/version strings. A separate fixed Docker capture records raw container/image inspection JSON, exact effective commands, pinned image digests, copied input FASTA/output bytes, `network_mode=none`, terminal exit 0, engine version, and logs. Capture manifest `localcontainermanifest_9a94a634e3b7e7c4a132c34ee4b352b62f865f836dd01dd7a370f5afeb01b1cd` authenticates both real full-catalogue containers, and the portable importer revalidates every digest and the exact raw proteome identity. The complete locked gate and exact-source CI `33159922527` pass. |
| `PH3-P1-27` Failed/excluded localisation could not follow the approved wave policy | Fixed and exact-source CI-qualified; first MR wave pending | Authenticated per-sequence failure IDs distinguish a typed local tool failure from unexplained missing/truncated output; only the former becomes neutral. The real catalogue policy retains 223 exclusions, 88 neutral groups, and 60 conflicts without dropping an identity. Excluded model/copy hypotheses are emitted completely in `deferred_localisation_hypotheses.jsonl`. The production first-copy graph now publishes one independently validated reopen plan per proceeding crystal; only complete terminal coverage with zero packed results can queue at most 175 retained hypotheses for the separately authorised pass-2 no-A expansion. Packed, failed, missing, duplicated, or cross-policy waves schedule none. The complete locked gate and exact-source CI `33159922527` pass. |
| `PH3-P1-28` Wrong-C negative control did not test real promotion or identity | Fixed and Marmic-qualified | The wrong model matches the frozen P6/3U7Q source-preparation, sequence-group, coordinate, uncertainty, and negative-control manifest and differs from every positive 9ECN model. Exact-source Marmic job `635840` completed it as a no-hit; its real schema-v2 assessment remains `search_evidence_only`, ineligible, and unclaimed. The same run recovered positive `2A+2B+2C` and independently validated all 61 Phase III checksums. |
| `PH3-P1-29` General composition execution omitted authority, diffraction, failure, and resources | Fixed locally; fixed-profile qualification pending | The real attempt process uses `process_mr`, reconciles current route/placement adapters and Phenix executable hashes with the Phase III identity, verifies the exact crystal MTZ and Free-R membership, consumes parent score/derivation/combined-coordinate evidence, and includes diffraction selection, preflight, rendered parameters, and parameter checksum in search identities. Infrastructure/contract outcomes now fail the task after retaining diagnostics; scientific no-hit and candidate-local tool/parse outcomes remain normal. |
| `PH3-P1-30` One-fixed A-to-B path was declared multi-fixed-qualified | Fixed locally; general-attempt qualification pending | Depth-two expansion now uses the already qualified fixed-parent partner adapter with explicit Phase III diffraction selection. Only C--F use the positive-9ECN-qualified multi-fixed adapter, whose schema again requires at least two fixed components. Focused command and rejection regressions pass. |
| `PH3-P1-31` Unknown-profile stage failures required a successful-output inventory for collection | Fixed, CI-qualified, and remotely exercised | The first exact unknown-discovery staging attempt stopped before scheduling because its frozen P0 site configuration was absent. Collection then incorrectly required the successful-run dynamic output manifest. Exact-source CI `33171592458` qualified the correction; deployed collection of failed job `636168` then retained phase/failure state, Nextflow logs, rejected `.command.run`/`.command.sh`, complete input authority, and failure signature without a successful-output manifest. Success or unclassified/active runs still fail closed, and any present inventory remains fully authenticated. |
| `PH3-P1-32` Lost database runtime configuration had no reviewed restore route | Fixed, CI-qualified, and remotely exercised | Storage maintenance preserved the immutable database root/manifest but removed the dispatcher configuration, blocking P0 restaging. Exact-source commit `ec1aaef`, CI `33174793722`, deployed the create-only `database-runtime-configure` operation. It restored runtime authority `6aaf03a…` without a build/download/scheduler action; the exact seven-input P0 bundle and new P0 authority `a5c06d…` then staged successfully and readiness returned true. |
| `PH3-P1-33` Provider-local tasks had no Slurm time limit | Fixed, CI-qualified, and remotely exercised | Unknown-discovery job `636168` reached Nextflow but Slurm rejected `RESOLVE_PROVIDER_PLAN` before provider execution because all five `process_local` modules lacked a defined resource class. Commit `b8adf02`, CI `33180768680`, defines 1 CPU, 1 GB, and 10 minutes. Successor job `636321` submitted the provider plan and validators with complete Slurm resources; its later input-contract failure is independent. |
| `PH3-P1-34` Restaged P0 content left stale remote MTZ paths in the private authority | Fixed and exact-source qualified | Authority attempt 8 binds all three restored P0 MTZ paths/checksums. Discovery job `636570` authenticated the 38-file input bundle and all three crystallographic routes, then completed successfully. |
| `PH3-P1-35` Discovery/screen collection omitted most input-authority files | Discovery side exact-source qualified; screen pending | Collection from job `636570` revalidated and transferred all 38 private authority members plus the complete provider output inventory and raw logs. The parent-bound screen copy/collection remains to be exercised. |
| `PH3-P1-36` Full-catalogue Foldseek aborted on an unmapped PDB target | Fixed and exact-source qualified | Job `636570` completed all 1,621 typed Foldseek results across 13 batches without aborting on database mapping gaps; all raw results/logs and the merged package were collected. |
| `PH3-P1-37` Unknown parent/child execution authority could cross source, P0, catalogue, or database state | Discovery side exact-source qualified; child pending | Job `636570` authenticated exact source/tree, nf-helper, Pixi lock, P0-confined MTZs, catalogue/database inputs, strict Phenix, localisation, and owned reviews. The parent-bound screen and single-component checks remain to be exercised. |
| `PH3-P1-38` Login-staged coordinate copies were unused and uncollected | Fixed locally; screen qualification pending | Login staging now publishes separate PDB/AFDB coordinate records pointing to the run-owned copied objects, validates every owned path/checksum after atomic publication, and the offline workflow consumes only those records. The screen collector authenticates and transfers every manifest-listed preparation file instead of retaining only `provider_preparation.json`. |
| `PH3-P1-39` Joint multi-copy A solutions were reset to one copy during continuation | Fixed locally; licensed workflow qualification pending | Schema-v2 A staging now parses the copied first-copy result, preserves the actual placed-copy count, requests rescue placement only below the expected count, and carries the count into T12. A joint `2A` regression schedules no redundant add-copy task, refines with input count two, and emits a zero-transition typed copy assessment. |
| `PH3-P1-40` Unknown fixed profiles lacked concurrency, replay, route, and complete collection enforcement | Discovery side exact-source qualified; screen/single pending | Job `636570` fanned out all 13 independent scheduler-managed batches, retained complete first/resume task identities, and produced byte-identical outputs. Screen A-package and single-component continuation enforcement remain to be exercised. |
| `PH3-P1-41` Pass-1 terminal evidence was not constructible from the real mixed workflow | Fixed locally; exact-source pass-1 qualification pending | The fixed derivation command now accepts no caller status or scores. It authenticates the completed scheduler manifest/result, execution identity, crystallographic/A/sequence/composition packages and decisions, package-owned copy JSONL, Phaser packing, coordinates, refinement, maps, and sequence result. Mixed credible/no-hit/failure crystals share the final single-component parent without sibling promotion, and the strict collector replays current v3 plus immutable historical v2 evidence. Focused terminal/collector tests pass; human A/final decisions and exact-source execution remain. |
| `PH3-P1-42` M6 operational/leakage execution contract was not the reviewed sequential shared-cache protocol | Fixed locally; PH6 real qualification pending | Leakage staging requires the collected successful same-source/site operational parent and binds its rehashed precheck plus operational cache before submit. Track-aware evidence permits cache reuse only for truthless import/PDB/Foldseek tasks, requires every track-specific task to execute, and requires a fully cached resume. Final collection independently rehashes the parent precheck and matches every reused task hash and child-file inventory. Staging enforces Viper/`main` or Marmic/`dev/phase3`; cross-site branch combinations fail before transfer. Coordinate materialisation remains explicitly offline and fails on a cache miss, and bounded-memory partitioning, aggregation, and collection remain unchanged. Focused M6, client, wrapper, and parent-binding regressions pass locally; observed mutation qualification and both real tracks remain pending. |
| `PH3-P1-43` RG7 closure and unknown pass 2 had contracts but no runnable fixed path | Fixed locally; RG7 evidence and exact-source qualification pending | The fixed `unknown-pass2` profile now accepts only a mode-0600 deterministic archive whose extracted tree, exact execution identity, every closure evidence file, and source commit/tree revalidate. Credible A states run one 25-attempt-per-depth, beam-width-three B--F chain under the 100-attempt/depth-six bounds; no-A crystals run the retained cumulative 175-hypothesis expansion and stop at a new owned A review. Terminal depths emit scope/assessment records and owned composition/sequence packages, depths four-to-six are forced provisional, and all automatic identity/composition claims remain false. Cached stub, focused scientific, wrapper, and fake-Marmic resource gates pass. |
| `PH3-P1-44` Copy-count evidence hash referred to an unpackaged one-row extract | Fixed locally; pass-1 qualification pending | Current derivation selects the reviewed state from the package-owned complete copy-assessment JSONL and binds that file's checksum. The collector independently selects the same unique state and rejects missing, duplicate, or malformed records. |
| `PH3-P1-45` RG7 closure trusted self-asserted evidence hashes | Fixed locally; real closure pending | Closure verification now rehashes the adverse review, integration gate, known controls, M6, pass 1, and exact-source CI files. It parses the CI record and cross-checks run/job/head/conclusion; a changed evidence byte fails. |
| `PH3-P1-46` Initial A state had no general component-coordinate/runtime bridge | Fixed locally; real pass-2 seed pending | A credible pass-1 result can now produce a depth-one refined, identity-unresolved `CompositionState`, exact component-A coordinate partition, source MTZ/runtime binding, and checksum manifest. One-component joint-copy partitioning uses the same installed-runtime-qualified per-placement contract as 6RTZ/3U7Q/9ECN. |
| `PH3-P1-47` Initial 25-A cap discarded the remaining active hypotheses | Fixed locally; pass-2 no-A qualification pending | Funnel v4 retains every active cap-deferred hypothesis separately from localisation exclusions. After complete zero packing, no-A expansion schedules cap-deferred rows first and then excluded rows, conserves both inventories, and remains capped at 175 additional attempts while Slurm owns concurrency. |
| `PH3-P1-48` All-model registry omitted executable model uncertainty | Fixed locally; fresh discovery pending | Registry v3 records exact model sequence identity, uncertainty source, and evidence digest for every model (predicted exact models use identity 1; PDB models use the owned mapping). Candidate planning distinguishes available models from unusable zero-identity models, and every B--F command binds the retained uncertainty. |
| `PH3-P1-49` Strict Phenix policy had no reviewed one-time migration route | Fixed and exact-source qualified | Probe job `636484` correctly rejected the legacy manifest. The reviewed create-only migration produced strict manifest `60d9b222…`, and exact-source successor job `636509` verified all seven executables plus the fixed Phaser interface without scientific execution. Scientific wrappers remain verify-only. |
| `PH3-P1-50` Staged Phenix migration failure log was unreachable | Fixed and exact-source qualified | The real migration failed before publishing its strict binding, but bounded `logs` returned an empty unsubmitted profile log instead of the run-owned migration evidence. A staged `phase3-phenix-probe` now exposes strict-verification evidence when present and otherwise migration evidence, without changing normal scheduler/profile log routing. Commit `7a3d57e`, CI `33239172950`, deployment, and the retained real migration log accept this boundary. |
| `PH3-P1-51` Verbose help displaced the failing migration result from bounded logs | Fixed and exact-source qualified | Even a 2,000-line tail began after earlier command results because Phenix help is extremely verbose. Staged verification logs now project the seven command headers and exact path/hash/arguments/exit/result/reason fields, while migration command stdout/stderr is retained separately. Commit `2446da0`, CI `33241613364`, deployment, the seven-command real summary, and the exact captured failure accept this boundary. |
| `PH3-P1-52` Strict manifest temporary file lacked a JSON suffix | Fixed and exact-source qualified | All seven installed Phenix probes passed, then schema loading rejected `phenix-strict-manifest.tmp.<pid>` because its format could not be inferred. The run-owned temporary filename now ends in `.json`; migration produced strict manifest `60d9b222…`, and successor job `636509` verified it successfully. |
| `PH3-P1-53` Bounded Foldseek batches inherited whole-catalogue 100-CPU/2-TB resources and serial execution | Fixed and exact-source qualified | Replacement job `636570` completed all 13 scheduler-managed batch children together at 32 CPUs/192 GB/four hours; observed peak RSS was 21.1--22.2 GB and all resumed as cached. The shared whole-catalogue class is 64 CPUs/192 GB/24 hours; only database construction retains 2 TB. |
| `PH3-P1-54` Active cancellation omitted its persisted failure class | Fixed and exact-source CI-qualified | Active cancellation now records `unknown_failure` while preserving an already completed run's status; focused remote lifecycle tests and CI `33261882387` pass. The historical pre-fix cancelled run remains non-reusable and intentionally unpromoted. |
| `PH3-P1-55` Unknown-screen stage logs were invisible through bounded diagnostics | Fixed and exact-source qualified | Bounded provider acquisition failed before scheduler submission, but `logs` returned empty despite a retained acquisition log. Commit `a2e4fb8`, CI `33275315980`, and the deployed bounded log interface recovered the exact run-owned acquisition failure without a new scheduler action. |
| `PH3-P1-56` Local collection required Slurm evidence for pre-scheduler stage failures | Fixed and exact-source qualified | The same deployed commit authenticated and collected the exact `stage_failed` archive from run ending `1759da00` using its owned manifest, phase, non-success class, absent scheduler evidence, and retained diagnostic log. Scheduled terminal validation remains unchanged. |
| `PH3-P1-57` Coordinate staging treated retained unmapped hits as selected mappings | Fixed and exact-source login-qualified | Commit `0ea68c4` and CI `33276701627` preserve deferred unmapped evidence while validating and acquiring only selected hits. Exact-source screen staging from discovery job `636646` completed that boundary and produced the full 25-mapping preparation without rejecting any retained deferred row. |
| `PH3-P1-58` Shared PDB files dropped later entity-specific coordinate records | Fixed and exact-source login-qualified | The real 25-mapping inventory selects multiple chain/entity records from four shared PDB entries. Exact-source login staging retained all 25 coordinate-source rows while copying 18 unique mmCIF byte objects, proving one shared object no longer drops later entity records. Compute consumption remains gated by `PH3-P1-59`. |
| `PH3-P1-59` Owned coordinate paths remained anchored to the atomic staging directory | Fixed and exact-source handoff-qualified | Screen job `636699` independently validated the relocated provider-login v3 preparation on compute, proving the temporary absolute path was removed. The job then reached model preparation and exposed the distinct registration-confinement defect tracked as `PH3-P1-60`. |
| `PH3-P1-60` Portable coordinate paths escaped their provider registration package | Fixed and exact-source model-qualified | Screen job `636733` validated the confined provider preparation and completed experimental model preparation, proving provider-owned `coordinate_objects/...` paths survive atomic staging and remain inside the registration package. It then exposed the distinct Nextflow localisation-staging defect tracked as `PH3-P1-61`. |
| `PH3-P1-61` Validated localisation bundle became an unsafe downstream symlink | Fixed locally; exact-source successor pending | Nextflow staged the physical output of `VALIDATE_PHASE3_LOCALISATION_BUNDLE` as a directory symlink in each first-copy funnel. The localisation importer correctly rejected that root. The funnel now uses Nextflow `stageInMode 'copy'`, preserving strict symlink rejection while giving each task owned bytes; a contract regression, syntax gate, and complete three-crystal cached stub pass. |
| `PH3-P1-62` Pass-2 closure was not anchored to the staged source or exact parent | Fixed locally; exact-source CI and remote qualification pending | Pass-2 input bundle v2 now requires the staged commit, resolved Git tree, successful single-component parent run, and exact tracked-ledger checksum. Every assessment must name that parent; every execution identity and the closure must name that commit/tree. The remote checkout independently re-derives the same source/tree/ledger before accepting the archive and rechecks them before submit. Focused local-builder, extracted-tree, client-propagation, and fake-dispatcher regressions pass. |
| `PH3-P1-63` Pass-2 accepted an incomplete frozen crystal panel | Fixed locally; exact-source CI pending | The private authority now requires exactly one item for each of `AD4QS1P4G2_18`, `CD4QS2P2G1_15`, and `CD6QS2P2G1_5`; duplicates, omissions, and substitutions fail before archive creation. The archive round trip retains all three sorted identities, and a two-crystal mutation regression fails closed. |
| `PH3-P1-64` Leakage baseline evidence rejected its required shared-cache hits | Fixed locally; PH6 real qualification pending | The leakage first pass intentionally reuses truthless catalogue-import, PDB-search, and Foldseek tasks, but the child-output and resource validators previously required every first-pass row to be `COMPLETED`. Track-aware schema-1.1 evidence now requires those shared processes to be `CACHED`, every track-specific task to be `COMPLETED`, and every resume task to be `CACHED`; any inverted or additional reuse fails. Focused mixed-status, child-integrity, and wrapper regressions pass. |
| `PH3-P1-65` M6 source branch was not bound to its reviewed site | Fixed locally; PH6 real qualification pending | Both M6 staging routes accepted either `main` or `dev/phase3` independently of the selected endpoint. The controller now accepts only Viper/`main` or Marmic/`dev/phase3` before archive inspection or transfer. Positive pairs and both cross-site negative pairs pass for input qualification and scientific staging. |
| `PH3-P1-66` Final M6 collection ignored the leakage operational-parent binding | Fixed locally; PH6 real qualification pending | Remote staging retained the operational run ID and four-file precheck digest, but truth-side collection previously accepted paired same-source/site tracks without reading either field. Collection now requires the exact operational run ID, independently rehashes the parent precheck, and matches every reused truthless task hash and complete child-file inventory. Missing, wrong-parent, wrong-precheck, and wrong-cached-task regressions fail closed. |
| `PH3-P1-67` M6 cache invalidation gate represented digest sensitivity as observed mutation | Open; checksum-bound observed qualification required | `cache_invalidation_verified` currently proves only that changing a declared input digest changes the aggregate key. The local 26-task mutation stub is useful regression evidence but is not a fixed-profile observed mutation record and cannot close PH6. Do not mark M6 accepted until a separately owned, checksum-bound real mutation record is generated and independently consumed. |
| `PH3-P1-68` Single-component handoff expected a nonexistent terminal timestamp field | Fixed locally; exact-source handoff qualification pending | The fixed job wrapper publishes canonical `completed_at`, while the handoff required `finished_at` and therefore rejected every genuine successful screen. The consumer now accepts only `completed_at`; a real wrapper-shaped success record completes the handoff, while the obsolete alias fails closed. |
| `PH3-P1-69` Single-component handoff omitted its required empty registry directory | Fixed locally; exact-source handoff qualification pending | The owned-run registrar deliberately requires an existing empty output directory, but the remote handoff passed an absent `owned_run_registry`. The handoff now creates that one directory before registration, and the real success regression verifies the resulting registry, A stage, hypotheses, and reviewed route. |
| `PH3-P1-70` Fixed screen wrapper rejected valid crystallographic holds | Fixed locally; exact-source mixed-screen qualification pending | The scientific graph correctly emits one held-crystal record instead of an A package, but the wrapper required three A packages. It now conserves exactly three distinct reviewed crystal IDs across owned A packages and retained holds. A two-package/one-hold lifecycle succeeds; a duplicated hold/package crystal with one missing sibling fails. |
| `PH3-P1-71` Unknown controllers had no runtime slack beyond their MR children | Fixed locally; dynamic-resource exact-source scheduler qualification pending | Dynamic MR children may consume a 24-hour first attempt plus one 48-hour retry. `unknown-screen`, `unknown-single-component`, and five-depth pass 2 therefore request 96, 120, and 384 hours; discovery remains 24 hours. Focused dispatcher assertions protect all bounds so a reviewed child/retry path cannot legitimately outlive its owner. |
| `PH3-P1-72` Nextflow imposed job-count caps above Slurm | Fixed and live-trace-qualified; successful-screen acceptance pending | The user directed that Slurm own concurrency. Phase III first-copy, refinement, no-A, and composition-beam processes no longer set `maxForks`; Marmic overrides the inherited executor queue capacity with Nextflow's unlimited value `queueSize = 0`. Job `637180` recorded `maxForks=0` and executor capacity zero, proving its 54-task inventory was produced by the scientific funnel rather than scheduler admission. Scientific attempt, depth, beam, and finalist budgets are unchanged. A successful 75-task one-copy screen remains required. |
| `PH3-P1-73` Parameterised list casts failed during live Nextflow process script construction | Fixed and exact-source CI-qualified; successful-screen acceptance pending | Unknown-screen job `636956` authenticated its exact source, provider preparation, reviews, localisation, and strict seven-command Phenix runtime, then submitted 54 first-copy hypotheses. After the first complete 25-result branch reached localisation reopen, Nextflow rejected `item[2] as List<Path>` with a zero-argument `_as_type()` error before creating the reopen task and killed ten still-running children. This is an execution failure, not a scientific no-hit; no A review is valid. Parameterised runtime casts are removed from all five affected active process script blocks, one live non-stub reopen regression executes the formerly failing script path, and a structural contract rejects their reintroduction. Exact-source CI run `33446167785`, job `99665648093`, passed on `f197700e674e93bc61382610602bf807bb0db269`. |
| `PH3-P1-74` Phase III MR tasks used one fixed allocation and resource failures had no bounded escalation | Fixed and exact-source CI-qualified; future Marmic qualification pending | Each active Phase III MR task now carries one content-addressed pre-execution workload plan over reflection count, moving atoms and searched copies, fixed atoms, and symmetry capped at eight. First attempts use 4/16/12, 6/24/18, or 8/32/24 CPU/GB/hour tiers. One retry is allowed only for exit 75 or scheduler/resource interruption codes 104, 130--145, and 175--177; resources scale linearly with `task.attempt` under 16 CPU, 64 GB, and 48 hour limits. Phaser independently verifies its thread allocation and retains the plan/attempt in command evidence. M6 remains fixed, unknown outcomes never tune tiers, and no retry creates a new scientific hypothesis. Exact-source CI run `33441583997`, job `99650836948`, passed on `511bbcdfe61fe3009daaf172277ff8e40b126781`; no Marmic child has yet exercised the dynamic tiers. |
| `PH3-P1-75` Localisation reopen read a noncanonical funnel-manifest name | Fixed and exact-source CI-qualified; Marmic qualification pending | Screen job `637180` completed the first 25-result crystal branch, then `PLAN_PHASE3_LOCALISATION_REOPEN` looked for `diverse_first_copy_funnel_manifest.json` even though the canonical funnel has always published `funnel_manifest.json`. The adapter now consumes that sole canonical file, and the focused unit fixture uses the producer's real contract. No scientific threshold or outcome changed. Exact-source CI `33446167785` / `99665648093` passed on `f197700e674e93bc61382610602bf807bb0db269`. |
| `PH3-P1-76` Live MR review lacked its canonical per-hypothesis result-root layout | Fixed and exact-source CI-qualified; Marmic qualification pending | The same first complete branch reached `BUILD_PHASE3_MR_SEED_REVIEW`, whose validator requires `first_copy_phaser_<hypothesis-id>` directories. The workflow supplied `phase3_first_copy_<crystal-id>_<hypothesis-id>` directories without constructing that review-root layout. It now validates each run-owned name and copies every complete bundle under the required canonical name before review. A non-stub Nextflow regression executes the live process with two real task-directory names. Exact-source CI `33446167785` / `99665648093` passed on `f197700e674e93bc61382610602bf807bb0db269`. |
| `PH3-P1-77` Failed unknown-screen collection omitted completed child MR evidence | Fixed locally; exact-source failed-run qualification pending | Job `637180` submitted 54 MR children and recorded 45 exit-zero completions, but failure occurred before the successful-output checksum inventory was published. The reviewed wrapper now builds a bounded per-child package on either first-run or resume failure. It conserves all funnel hypotheses, attempts, scheduler IDs, completion/timing state, allocated Slurm directives, schema-validated result state, command evidence, Phaser assets, resource plans, file checksums, and omissions. Remote collection independently authenticates every safe regular file and rejects tampering. The package is permanently marked non-scientific and non-reusable; the historical 45 tasks remain execution observations only. Focused unit, wrapper-policy, two-path dispatcher, checksum, and tamper regressions pass. A fresh exact-source failed-screen exercise remains required before RG7. |
| `PH3-P1-78` The fixed pass-1 funnel did not emit the required 75 hypotheses | Fixed locally; exact-source CI and 75-task Marmic qualification pending | The uncapped trace for job `637180` proved the scientific funnel emitted 25 AD4, four CD4, and 25 CD6 hypotheses because joint initial MR discarded expected copy counts above four. Adapter v6 deletes the joint-copy option and four-copy admission filter, retains the Matthews expectation, searches one copy, and builds the dynamic resource plan from that one-copy workload. Static four-copy restrictions are removed from the A-screen, placement, assessment, per-placement, fixed-component, and current component-execution contracts; further copies remain sequential and human-gated. Focused ranking/schema/workflow tests pass, and the checksum-frozen outcome-blind audit now selects 25/25/25 hypotheses; CD4 includes expected counts 4--12 and 16 while every searched/resource-plan copy count is one. The fixed successful wrapper requires exactly 25 MR tasks for each proceeding crystal and zero for a held crystal. No unknown outcome or score tunes the ordering. |
| `PH3-P2-01` Collected active Phase III records retained dead machine paths | Fixed locally; collection qualification pending | Provider packages, single-component routes, finalists, composition manifests, and pass-2 review records now carry package-relative paths. Raw command/log bytes remain evidence but no portable package depends on their transient execution paths. |
| `PH3-P2-02` Large scientific collection buffered the complete archive in RAM | Fixed locally; large-run qualification pending | The reviewed client streams collection stdout to one bounded `/tmp` archive, validates terminal evidence directly from that file, and extracts members incrementally. The collection ceiling is 12 GiB; failures never read an oversized partial archive into memory. |

## Updated 2026-08-25 fail-closed and clean-break review

Reviewed exact source: `26e69b95d57d6c8fd543980b661fc055b08bbcc8`.
The [independent adverse review](adverse-code-review-2026-08-25-fail-closed-clean-break.md)
provides each source location, mechanism, and negative case. The
[remediation roadmap](phase-iii-fail-closed-clean-break-roadmap.md) defines the
ordered `RG0`-`RG7` milestones and both scientific stop gates.

| Finding | Current disposition | Required gate and acceptance evidence |
| --- | --- | --- |
| `FCB-P0-01` Fabricated final Phaser packing | Fixed locally; exact-source CI and installed-control qualification pending | RG2; first-copy, additional-copy, and partner searches now require either explicit final packing counts or an observed final `PAK=0` annotation. Missing/nonzero log and coordinate evidence fail closed, all five affected adapter/cache identities advance, and 69 focused Phaser/copy/partner/control regressions pass. |
| `FCB-P0-02` Self-asserted credible unknown assessment | Fixed locally; exact-source CI and owned-HPC qualification pending | RG3; credible pass-1 status now requires independently owned crystallographic, A-seed, sequence, and composition packages/decisions plus exact terminal, copy-count, Phaser-packing, refinement, sequence-map, and residual/metric contracts. The collector parses and cross-binds the exact crystal, parent, execution, state, sequence, copies, final R values, coordinates, complete refined MTZ coefficients, and CCP4 review map. Missing sequence approval, checksum-matched placeholder evidence, cross-run/crystal/state/group identities, invented packing/R values/residual status, and invalid scientific assets fail closed; all 45 focused assessment/collection cases pass. |
| `FCB-P0-03` Unresolved Free-R test convention | Fixed locally; exact-source CI and owned unknown-input qualification pending | RG2; Phase III dispatch now rejects a missing test value before publishing its diffraction identity, and the private unknown-discovery bundle requires an exact three-crystal manifest carrying each explicit value. That manifest is archived, checksummed, copied to the offline screen, and used instead of mutating frozen P0 metadata. All command contracts still require the explicit value; no distribution-based inference exists. The three frozen MTZ histories identify CCP4 FREERFLAG at 5%, whose official convention defines test flag 0; local exact-input dispatch now records explicit value 0 for all three crystals, including CD6's distinct Free-R dataset 0. The final owned manifest and review package must retain that evidence. Focused missing-value, archive, dispatch, schema, and real dispatcher-chain regressions pass. |
| `FCB-P0-04` Optional reviewed provider policy | Fixed locally; exact-source CI and owned-HPC qualification pending | RG1; all PDB, Foldseek, and AFDB application adapters and CLI entry points now require the exact enabled, checksum-bound provider plan/entry before execution. Login staging resolves and retains the reviewed plan; provider/cache identities bind explicit authorisation. The separately frozen truthless M6 route independently authenticates its task, site policy, lock, database, threads, and cache identity while preserving 25 raw proposals before leakage filtering and the three-accepted-hit cap. Missing, mixed, wrong-provider, changed-provenance, changed-cap, and missing-login-plan cases fail closed; 115 focused provider/M6 cases and the reviewed dispatcher regressions pass. |
| `FCB-P0-05` Executable cross-crystal legacy report | Fixed locally; exact-source CI and owned-HPC qualification pending | RG3; both legacy scientific-status/report writers, their public CLI routes, and their self-asserted cross-crystal/sequence approval tests are deleted. One explicit read-only historical checkpoint verifier preserves strict checksum/path/symlink validation without writing or promoting any claim, and retained legacy resource evidence remains readable. Two red/green unsupported-command cases, immutable historical-reader and confinement regressions, and the owned schema-v2 reporting suites pass. |
| `FCB-P0-06` Missing mandatory final review channels | Fixed locally; exact-source CI and owned-HPC qualification pending | RG1; reviewed continuation now refuses a missing or shared single-component owner before scheduling, and its workflow requires the owned screen registry, exact execution identity, distinct parent, and both final review packages. Two red/green parent regressions and owned three-crystal fan-out preserve genuine approved and deferred outcomes without empty mandatory channels. |
| `FCB-P0-07` Self-asserted component/composition identity | Fixed locally; exact-source CI and owned-HPC qualification pending | RG3; placed component identity now remains unresolved unless its exact owned sequence package, independent approval, source model, copied map, parsed sequence-map result, refinement, crystal, execution, parent, copies, and component identities all agree. A distinct owned composition package/approval must independently bind the ordered models, sequence groups, copies, observed coordinates, parsed map/refinement, and actual checksum-qualified state before a complete claim. The frozen wrong-B TFZ 5.1/LLG 327.049, opaque decision hashes, reviewer non-approval, cross-owned records, placeholder maps, and unsupported exact-locus promotion fail closed; typed packed/search outcomes and equivalent-sequence groups remain available. |
| `FCB-P0-08` Missing pre-pass-2 finding closure barrier | Fixed locally and wired to the fixed profile; final RG7 evidence pending | RG0/RG7; one content-addressed closure record binds the exact source commit/tree, ledger bytes, adverse review, integration gate, known controls, M6, unknown pass 1, exact-source CI, and one sorted final entry per finding. The validator now rehashes every named evidence file and parses the CI run/job/head/conclusion rather than trusting declared digests. The fixed `unknown-pass2` archive and every depth-planning task require this exact closure; absent, stale, changed, non-final, or cross-source state refuses scheduling. Earlier contract CI remains historical; the final exact-source closure has not yet been created. |
| `FCB-P0-09` No reviewed fixed unknown HPC profiles | Application/discovery/screen exact-source CI-qualified and single-component fixed locally; final wrapper CI/HPC qualification and gated pass-2 remain | RG1; the user approved a compute-only `unknown-discovery`, bounded login-coordinate staging, and offline `unknown-screen` chain. Discovery validates the three-crystal review authority, runs only local PDB/MMseqs2 plus deterministic full-catalogue ProstT5/Foldseek, and publishes one complete content-addressed owned package. Login staging consumes only that package, fixes direct PDB at three hits/group and 25 mappings globally, permits AFDB by explicit accession only, retains typed disabled ESM results, and inventories each referenced coordinate object without public sequence submission. Offline first-copy independently matches discovery, preparation, and execution identities before model preparation/MR and schedules no provider search, download, or coordinate-registration task. Exact-source CI `32926373409` / `98050008356` passed on `a6d8fd1` under Pixi 0.76.2. Discovery and screen wrappers enforce path-free private input, terminal parent ownership, fixed 8-CPU/32-GB/24-hour resources, login-only acquisition, offline traces, and cached replay; exact-source CI `32929375446` / `98058501395` passed on `e87c679`. The single-component wrapper accepts at most three checksum-confirmed A-seed TSVs from the exact successful screen, builds the canonical owned registry/stages/routes, and runs only the reviewed continuation. Eighteen focused package/login/offline/private decision cases, 158 neighbouring HPC cases, and the real dispatcher discovery-through-single terminal chain pass. Final wrapper CI, deployment, real HPC qualification, and gated pass-2 remain before final disposition. |
| `FCB-P0-10` Legitimate cross-dataset Free-R flags rejected | Fixed locally; exact-source CI and real refinement qualification pending | RG2; the frozen CD6 MTZ stores its sole integral `FreeR_flag` in base dataset 0 while the selected observations belong to dataset 1. The former equality assertion made the real Phase III dispatch fail before MR. Dispatch now resolves the unique exact column from the same source MTZ, records observation and Free-R dataset IDs independently, and retains the complete distribution and HKL-to-flag membership digest. An explicitly supplied wrong dataset still fails, duplicate labels remain ambiguous failures, 20 focused Free-R/dispatch cases pass, and the exact frozen CD6 dispatch now succeeds. The convention remains unresolved until separately authoritative test-value evidence is bound. |
| `FCB-P1-01` Malformed sequence output becomes no-hit | Fixed locally; exact-source CI and installed-runtime qualification pending | RG2; sequence-from-map requires exactly one complete finite terminal score summary, rejects malformed/non-finite score rows and unsupported positive no-hit summaries, preserves explicit zero-score outcomes, and advances both refinement protocol identities. Five red/green malformed-output cases and 66 focused refinement/review regressions pass. |
| `FCB-P1-02` Lossy scientific coordinate/log decoding | Fixed locally; exact-source CI and installed-control qualification pending | RG2; all first-copy/additional-copy/partner authoritative Phaser logs and coordinates now require strict UTF-8, corrupt evidence becomes a typed failure without placement claims, capture bytes are retained exactly, and all five scientific adapter identities advance. Seven red/green corruption cases, authoritative-versus-diagnostic capture controls, and 78 focused regressions pass. |
| `FCB-P1-03` Missing sequence mass becomes assessed | Fixed locally; exact-source CI pending | RG4; physical assessment is mandatory rather than defaulted, and assessed candidates require exact or complete bounded component mass. Two red/green absent-assessment/missing-mass regressions and 46 focused composition/planner/candidate tests pass. |
| `FCB-P1-04` Synthetic legacy Phase III approval records | Fixed locally and exact-source CI-qualified; owned-HPC qualification pending | RG1; the owned schema-v2 A checkpoint contains the complete checksum-bound MR review outputs and every per-solution scientific asset. Active staging now consumes only its canonical decision stage, registered package, execution identity, parent run, and hypotheses, then emits one content-derived schema-v2 seed-stage manifest, two seed tables, checksum-named models, and byte-identical owned package/stage snapshots. It never writes `approved_mr_seeds.tsv`, `validated_mr_seed_decisions.json`, or `live_m4_stage_manifest.json`. Current Phase III additional-copy and T12 adapters explicitly accept only this stage and reject legacy or dual approval authority; reviewed-crystal routes no longer carry an external legacy review directory. Historical fixed/normal-workflow controls retain their genuine v1 path. Fifteen ownership/tamper/disposition cases, 45 review-stage cases, 32 add-copy/refinement cases, ten unknown-wiring cases, one- and three-crystal cached replay, and the complete Nextflow syntax gate pass. Exact-source CI `32899255889` passed on `b615c34`. |
| `FCB-P1-05` Competing current application authorities | Fixed locally and exact-source CI-qualified; owned-HPC qualification pending | RG1; current execution now enters only through `phase3_application.nf` and one required typed operation: reviewed joint `first_copy` or owned `reviewed_single_component`. The root has no legacy approval input, joint-mode switch, or one-crystal compatibility selector. Archival `main.nf` retains only its v0.2 schema-v1 route, and both roots reject cross-authority parameters before scheduling. Four focused ownership/routing cases, both three-crystal application stubs, and the complete Nextflow syntax gate pass. Exact-source CI `32904417863` passed on `b55348a`. |
| `FCB-P1-06` Synthetic v1 all-model registry manifest | Fixed locally; exact-source CI and owned-HPC qualification pending | RG4; the all-eligible model universe publishes exactly one schema-v2 registry, canonical processed records, and content-addressed coordinates. Phaser independently validates the complete registry, exact owned processed-record path, sequence group, model identity, and every coordinate checksum before execution; Phase III refuses legacy/fabricated or competing authorities and binds the registry identity/digest into its immutable command. Current Phase III, diverse-search, and frozen M6 first-copy task paths consume that canonical registry directly; historical fixed controls retain their genuine original preparation evidence. One red/green obsolete-writer regression, four authority-substitution/tampering cases, 171 focused registry/Phaser/provider/composition/M6/partner tests, and the actual M6 and three-crystal cache/resume graphs pass. |
| `FCB-P1-07` Repeated inferred Phenix runtime migration | Fixed and exact-source qualified | RG4; one reviewed non-scheduled migration created strict manifest `60d9b222…`. Exact-source successor job `636509` verified all seven executable hashes and ran only `phenix.phaser --show_defaults`; `scientific_execution_performed=false`. Every scientific profile remains verify-only and pass-2 identity requires the strict runtime bindings. |
| `FCB-P1-08` Malformed/duplicate reviewed A inventory | Fixed locally; exact-source CI and canonical-v2 migration pending | RG1; the active A-seed stage now independently validates every legacy review row, rejects absent/non-object/non-string/duplicate solution identities, and conserves the complete source inventory before creating any output. Three red/green malformed/duplicate regressions, all approved/rejected/deferred ownership controls, and 51 focused A-seed/raw-loader cases pass; the separate legacy-bridge deletion remains tracked by `FCB-P1-04`. |
| `FCB-P1-09` Missing current HPC site silently becomes Marmic | Fixed locally; exact-source CI pending | RG1; current owned schema-1.1 records now require an explicit valid site, schema-1.0 historical evidence is Marmic-only, and absent/unknown versions fail. Four focused red/green invalid-schema cases and 76 owned-run/HPC client regressions pass. |
| `FCB-P1-10` Malformed/duplicate remote protocol fields | Fixed locally and exact-source CI-qualified; coordinated deployment pending | RG1; malformed/duplicate/noncanonical frames, mismatched operations, missing/cross-run ownership, and cross-site responses fail closed. Current status and log responses now emit the selected dispatcher site, and the client refuses either operation when that mandatory field is absent or differs. Thirteen focused status-protocol attacks, an independent missing-log-site regression, all 124 client tests, dispatcher Bash syntax, and the real fake-scheduler lifecycle pass. Successor exact-source CI `32934108654` / `98071929325` passed on `eb0eadc`; deployment remains intentionally deferred until the active old-protocol control is terminal and collected. |
| `FCB-P1-11` Missing failed-job evidence suppresses failure signature | Fixed locally; exact-source CI and owned-HPC qualification pending | RG1; every collected archive is validated before publication against its exact owned site, run, profile, iteration, immutable commit, source-versioned Pixi lock, helper provenance, numeric scheduler job, explicit terminal result, coherent failure/exit class, canonical timestamps, approved logs, and typed artefact inventories. Missing, corrupt, cross-owned, contradictory, duplicate, unsupported, or nonterminal evidence fails closed without publishing a collection; failed database archival and repeated-failure guards require genuine terminal evidence. Twenty red/green collection attacks, six direct signature regressions, successful/failed/cancelled controls, all 137 neighbouring HPC client/CLI cases, and the real dispatcher lifecycle pass. |
| `FCB-P1-12` Unsupported scheduler state becomes executing | Fixed locally; exact-source CI and owned-HPC qualification pending | RG1; reviewed waiting accepts only explicitly enumerated queue, execution, or terminal scheduler states and independently requires the exact consistent terminal flag before a phase transition. Eight red/green missing, unsupported, or contradictory-state regressions, an owned running-to-completed control, and 77 focused client cases pass. |
| `FCB-P1-13` Missing remote log payload becomes empty log | Fixed locally; exact-source CI and owned-HPC qualification pending | RG1; bounded remote log collection now requires the authenticated `logs` operation, exact owned run identity, and an explicitly declared base64 payload. Five red/green missing/mismatched ownership or payload regressions, one genuine zero-byte control, 68 focused owned-run cases, and two real dispatcher-log integrations pass. |
| `FCB-P1-14` Review checkpoint paths escape ownership | Fixed locally; exact-source CI and owned-review qualification pending | RG1; active legacy checkpoint/report consumers now use the existing strict schema-v2 relative-path contract, reject every intermediate symlink, and resolve every hashed asset inside its exact owned package. Three red/green parent-traversal and symlink-escape regressions plus 26 report, resource-summary, and Phase III review-package cases pass. |
| `FCB-P1-15` Compute-worker network denial remains unenforced | Fixed locally and exact-source CI-qualified; target-site qualification pending | RG1; both reviewed profiles now execute every in-job Nextflow task through a fail-closed `/usr/bin/unshare` user/network namespace and require a numeric Slurm job context. Marmic `run_local` and Viper `needs_internet` retain controller-local scheduling but do not restore ordinary Bash or grant network. Missing namespace support or a non-Slurm invocation fails without a fallback; the existing terminal collector separately authenticates scheduler ownership. The fixed argument-free Marmic probe binds the site configuration and shell checksums, then requires one distinct Slurm child and one outer-controller task to enter separate namespaces and receive a denied socket result against the fixed TEST-NET-1 address. Four probe-contract cases, 157 focused HPC unit cases, one real dispatcher stage/submit integration, Nextflow syntax, and wrapper syntax pass locally; exact-source CI `32908137245` passed on `18036c9`. Before pass 1, the real Marmic probe plus complete bounded pre-submit login acquisition remain mandatory; Viper requires its own qualification only before scientific use there. |
| `FCB-P1-16` Wrong-B fixed wrapper omitted parent uncertainty | Fixed locally and full-gate qualified; exact-source CI and replacement control pending | RG2/RG3; Marmic control `634369` completed the green 6RTZ, 3U7Q, and full-catalogue branches, then stopped before wrong-B execution because the fixed wrapper omitted the now-mandatory parent model identity and uncertainty arguments. The wrapper now derives both values from the checksum-bound 6RTZ parent Phaser command using the same validation applied to 3U7Q and passes them to the negative search. A focused command-contract regression, the complete fake 6RTZ/3U7Q/P6 lifecycle, and the full locked repository gate pass. The failed run remains diagnostic only because wrong-B, P6 assessments, and final checksums were not produced. |
| `FCB-P1-17` Network probe used a rejected nested main-script route | Fixed and Marmic-qualified | RG1; Marmic probe `634652` failed before task creation because Nextflow 26.04.6 interpreted `run <source-directory> -main-script <nested-file>` as an absent script path. The probe is now the tenth operation owned by canonical root `qualification.nf`, preserving repository-root `projectDir` for configuration, pinned environments, and the worker shell. Exact-source CI `32955931998` passed; accepted successor `634673` launched both qualified task classes through that route. |
| `FCB-P1-18` Network probe omitted its typed cache-root input | Fixed and Marmic-qualified | RG1; after canonical routing was fixed, Marmic probe `634663` reached strict Nextflow parameter validation but launched zero tasks because default `cache_root=.cache` did not exist in the empty execution directory. The wrapper now creates and passes one run-owned probe cache explicitly. Exact-source CI `32960948125` passed; accepted successor `634673` used the owned cache and completed both worker-denial tasks. |
| `FCB-P2-01` Competing root Nextflow application entry points | Fixed locally and exact-source CI-qualified; fixed-HPC qualification pending | RG5; nine public stage wrappers are replaced by one typed `qualification.nf` owner with an explicit operation. The reviewed HPC wrapper, public examples, stage documentation, syntax/stub matrix, and focused fake-dispatcher tests all use that owner. Unknown or incomplete operations fail before scheduling. Root policy now permits only archival `main.nf`, current `phase3_application.nf`, database preparation, M6, and qualification; the nine superseded roots are deleted after all replacement operations passed the complete Nextflow stub gate. Exact-source CI `32910230567` passed on `de2f4c4` under Pixi 0.76.2. |
| `FCB-P2-02` Permanently retired CLI execution aliases | Fixed locally; exact-source CI and integration qualification pending | RG5; all three retired direct control/M6 benchmark commands, parser branches, dispatch paths, fail-only functions, legacy request types, compatibility diagnostics, and their obsolete positive test are removed. The CLI rejects every former command during parsing; retained preparation/classification helpers, truthful historical evidence readers, M6 task boundaries, and fixed Nextflow controls remain available. |

## Closure rule

Before the second unknown-dataset pass, every original and new row in this
ledger must be `Fixed`, `Superseded`, or `Deleted`, with a focused regression,
exact-source commit/CI evidence, and any required fixed-HPC qualification.
`Open`, `Partial`, `fixed locally`, and `qualification pending` are not final
dispositions. The fixed second-pass profile must independently reject absent,
stale, incomplete, or source-mismatched closure evidence.

Before `v0.3.0`, rerun an independent adverse review against the exact release
candidate and verify the same complete final dispositions and evidence again.
