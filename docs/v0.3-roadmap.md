# v0.3 candidate-confidence-first roadmap

## Authority and status

This document defines the canonical execution order for the unreleased
`main` line after immutable release `v0.2.0`. Detailed Phase III method, finding, validation, and release
documents remain applicable, but they may not advance work ahead of the order
defined here.

The current published research release remains experimental `v0.2.0`.
`v0.3.0` is not complete and must not be represented as validated until every
release condition in this document is satisfied.

## Current operator decisions - 2026-09-09 and 2026-09-11

The user cancelled completion of the expanded identification screen, including
the AFDB supplement, and accepted all three crystal candidate assessments as
complete following supervisor confirmation of the rank-one candidates. The
cancelled programme is not an exhaustively completed screen; preserve its
coverage limits, native outcomes and cancellation evidence. External supervisor
confirmation does not fabricate refinement, map-derived sequence evidence or
formal workflow decisions that were not produced.

RF-G1 through RF-G4 are now the next priority. Follow the
[ranking-gate implementation plan](ranking-release-gates.md), then the remaining
M6, finding-closure, pass-2 and release qualification requirements. Do not launch
further AlphaFold work or re-open these accepted candidate assessments as a
development prerequisite. The older execution updates below are historical
where they conflict with this operator decision.

On 2026-09-11 the user explicitly defined the remaining second pass as a
rank-one recovery test on those same three frozen datasets: check whether each
supervisor-confirmed correct candidate ranks first again under the qualified
production ranking. This bounded retrospective test is authorised despite the
closed investigation; it is not permission to resume the cancelled expanded
screen/AFDB cohort or to infer new structural/identity claims. Keep the expected
candidate identities outside ranking inputs and compare only after output
checksums are fixed. Passing all three cases requires no further human approval;
any failed or inconclusive recovery pauses development/release progression for
the user after the evidence is preserved.

Unknown-crystal continuation and its A-seed, copy/composition and final identity
decisions are v0.4 work, not v0.3 execution prerequisites. The user plans further
samples after v0.4 development. Keep those workflow safeguards intact, but do
not invoke the existing composition-oriented `unknown-pass2` profile as if it
were this ranking-only test. The older execution/continuation requirements
below are historical wherever they conflict with this explicit scope.

## Prior scope and execution history

Approved successor work on 2026-09-08: v0.4 will add fixed brief refinement of
valid retained MR placements before final candidate filtering and A review,
after placement-validity and target-aware model-preparation checks. See
[BR01 in the v0.4 plan](v0.3-v0.4-development-plan.md#br01-brief-refinement-before-final-candidate-filtering).
This requirement is deferred to v0.4; it does not add a v0.3 release gate, alter
current refinement ordering, approve any current candidate, or launch a job.

The same v0.4 plan now includes
[AF01, the experimental-first AFDB Foldseek fallback](v0.3-v0.4-development-plan.md#af01-original-sequence-predicted-model-afdb-foldseek-fallback),
queried with an original-sequence predicted model and deferred for suitable GPU
access, and
[AF02, exact-sequence AlphaFold MR for retained finalists](v0.3-v0.4-development-plan.md#af02-exact-sequence-alphafold-mr-for-retained-finalists).
AF02 also checks finalists initially screened with PDB templates; it does not
apply to every screened catalogue candidate. The separate immediate request
to run supplied exact-sequence AFDB models on Raven requires a qualified
predicted-model execution path, preserves the completed PDB screen, and is
not implementation of these deferred automatic stages. No new GPU folding,
brief refinement or A-seed approval is implied.

Latest override on 2026-09-07: the user requires independent identification and
authorised implementation, push and deployment of the separate Marmic
[all-candidate identification screen](identification-screen.md). This profile
accounts for every catalogue sequence in the supplied experimental mass
intervals, including explicit existing-work reservations and model gaps. It
does not truncate acquisition at the old global 25-model pool. Initial search
copies and all scientific/human-review safeguards are unchanged; the general
mass-blind generator redesign remains deferred. No further local batch is to
be launched. Preserve the existing local batch, qualify a representative remote
execution and then run the remaining model-supported inventory on Marmic.
The representative remote route completed and its native evidence was collected
and classified without a supported identity. The full remaining model-supported
inventory is now running; initial preparation and MR submissions are confirmed.
Full scientific outcomes and candidate assessments remain pending.
The operator subsequently requested a temporary five-job total ceiling for this
identification programme: one controller plus four preparation/MR children.
The completed three-case smoke is preserved and not repeated. The full screen
uses the capped source; do not lift the cap automatically.

Previous override on 2026-09-06: the user authorised continued exploration of
distinct candidates. A new private 35-candidate batch is running, after
excluding sequence groups from the completed screen and both earlier local
batches. It retains existing initial-search semantics, complete Matthews
priors, and original diffraction inputs. Focused preparation, command, parser,
failure and inventory checks passed; two representative native tasks have
started. Further distinct bounded batches are authorised if needed, but no
new seed, copy-policy change, refinement or identity decision is inferred.
The previously asked full-expected-copy strategy question does not block this
newly authorised existing-policy exploration. The failed integrated
continuation remains diagnostic only; it was not restarted. All candidate
assessments and v0.3.0 remain incomplete.

Historical critical-path state on 2026-09-05: the five-state A-only policy at
`184c9aaea9de` passed the full locked local gate, all six CI lanes, reviewed
deployment, fresh discovery, and its parent-bound screen. All 75 MR requests
completed on their first attempt using the overprovisioned 8/12/16-CPU tiers.
All 98 workflow tasks replayed from cache with matching task identities and
identical pre/post-resume output manifests. The current owned A packages and
complete candidate lists are retained privately. Work is stopped at the
mandatory package-bound A decision; no crystal identity is established. The
user subsequently approved one exact seed for sequential continuation and
requested additional exploration for the other crystals. That exploration uses
five new candidates per crystal and has now completed independently without a
supported identification. All ten exploratory MR tests completed normally;
weak packed placements and completed no-solutions remain explicitly distinct.
The continuation
failed during dispatch before MR because its manifest omitted the already
selected explicit Free-R test value (`PH3-P1-85`); no replacement is submitted.
The user has also queried the distinction between Matthews expected ASU content
and incremental copy searches. A proposed change to searching the complete
expected count in one invocation awaits explicit confirmation. Do not infer
that strategy decision or relaunch completed exploratory work. The new
exploration authority above supersedes the earlier stop against further tests.
Unselected A targets
remain undecided and final sequence/composition review is still mandatory.

The successful successor uses candidate-specific dynamic copy ranges, the
checksum-pinned 2013 MATTPROB resolution-conditioned solvent density, a soft
published empirical `P(n)` ASU-copy weight, and independent Matthews and MR
ranks with visible discordance. It does not establish any identity. The user
must now explicitly approve, defer, or reject exact package targets before any
sequential-copy, refinement, sequence, pass-2, or composition work proceeds.
The earlier job `638943` remains immutable execution evidence, but its fixed
16-copy candidate selection and A gate remain superseded by `PH3-P1-81`.

The user subsequently approved a five-state A-only continuation round using
the corrected review-priority top five for each crystal and accepted AD4's
coupled two-copy tNCS states for this round. Composition beam width remains
three. Because the active schema accepted only three approved A states, the
five-state policy required a focused contract change, exact-source local/CI
qualification, and one fresh source-bound discovery/screen chain. Those steps
are complete. New package decisions remain required before continuation.

One material crystallographic boundary remains explicit in the new review:
every AD4 command requested one search copy, but Phaser `MR_AUTO` again detected
a two-member translational-NCS set and automatically edited all 25 AD4 searches
to two physical placements. CD4 and CD6 remained literal one-copy placements.
The user has explicitly accepted that coupled tNCS pair as an initial A state
for this round. Do not ask the same policy question again or mislabel the pair
as strict one-copy evidence.

The user's latest execution priority is case identification using separately
supplied experimental information. The general mass-blind hypothesis-generation
redesign is deferred until after v0.3; it must not become a prerequisite for
case-specific exploratory tests. A separately authorised local five-candidate
exploration has completed, with full private logs and no supported identity.
Retain the distinction between weak packed placements, completed no-solutions,
and parser failures with native negative evidence. Exploratory results are not
automatically release-qualified. New experimental hints require reassessment
of the previous candidate-selection plan, not silent reuse of its decisions.

## Goal

First produce the strongest scientifically defensible candidate assessment for
each of the three frozen unknown crystals:

- `AD4QS1P4G2_18`;
- `CD4QS2P2G1_15`; and
- `CD6QS2P2G1_5`.

The assessment must preserve all alternatives and may end at an exact sequence,
a sequence-equivalence group, a protein family, an unresolved shortlist, or an
explicit no-supported-candidate result. It must never force an identity.

Only after all three candidate assessments and their supervisor-ready reports
are complete may development advance through the remaining M6, finding-closure,
unknown-pass-2, final-report, atlas, packaging, and release work required for
validated research version `v0.3.0`.

## Scientific execution policy

### Initial screen

- Matthews enumeration derives and retains the complete candidate-specific
  expected-copy range from ASU volume, sequence mass, and configured solvent
  bounds; no configured scientific copy ceiling is permitted.
- Single-component ordering uses the checksum-pinned 2013 MATTPROB
  resolution-conditioned solvent density multiplied by the published
  homooligomer copy-frequency prior `P(n)`. This is a soft ranking weight, not
  an identity probability, and zero-weight states remain retained.
- Full catalogue sequence mass, not trimmed search-model mass, determines each
  expected copy hypothesis.
- The initial MR task searches exactly one copy, regardless of expected ASU
  copy count.
- No special four-copy filter, joint-copy execution mode, or hidden copy-count
  upper bound may remove an otherwise retained expected-copy hypothesis.
- The pass-1 execution budget remains at most 25 ranked hypotheses per crystal.
  The frozen current inputs produce at least 25 admissible hypotheses for each
  crystal under the one-copy policy, so the qualified screen must emit 75
  independent MR tasks.
- Slurm owns aggregate task admission. Nextflow may apply no global, profile,
  queue-size, or process-local concurrency ceiling to these scientific tasks.

### Resource policy

Each hypothesis receives a content-addressed resource plan derived before
execution from reflection count, moving-model polymer atoms, the one searched
copy, any fixed-component atoms, and crystallographic symmetry multiplicity.
At the operator's explicit request, successor first attempts deliberately use
overprovisioned 8/12/16-CPU tiers with 32/48/64 GB and 24/36/48 hours. One
classified resource/infrastructure retry still follows linear
`task.attempt` scaling and is bounded at 16 CPUs, 64 GB, and 48 hours.
Scientific no-hit, malformed evidence, and contract failure are not retryable
resource events.

### Sequential copy completion

Only an explicitly approved one-copy A seed may continue. Additional copies are
placed one at a time toward the retained Matthews expectation. Every supported
and unsupported intermediate state is retained. The workflow must not replace
this evidence chain with a simultaneous high-copy search.

### Confidence boundary

LLG, TFZ, and packing support candidate screening but do not establish
identity. A high-confidence assignment requires coherent evidence from:

1. a valid, reproducible MR placement;
2. physically and crystallographically credible packing;
3. sequential copy behaviour compatible with retained alternatives;
4. stable refinement using the original Free-R-bearing diffraction data;
5. interpretable maps and model-versus-data agreement;
6. sequence-discriminating evidence against the complete trusted catalogue;
7. explicit human review; and
8. complete provenance and checksums.

If those layers disagree or cannot discriminate candidates, the final status
must remain uncertain.

## Candidate-confidence critical path

### 1. Freeze and classify the failed predecessor

The terminal predecessor screen and every failed or cancelled predecessor are
diagnostic only. Their scientific caches are non-reusable. Retain their bounded
logs, collected controller evidence, exact failure signatures, and child-task
inventory without promoting any hit/no-hit or identity claim.

Acceptance evidence:

- scheduler, exit, source, parent, provider, input, and checksum ownership;
- exact submitted/completed/killed/unemitted task counts;
- explicit `scientific_evidence_accepted=false`; and
- no cache or result path reused by a successor.

### 2. Implement the one-copy initial screen

Remove the joint-copy option and the four-copy execution filter from the active
candidate funnel and workflow. Remove the separate configured copy ceiling and
derive the complete finite range independently for every candidate. Preserve
Matthews expected copy count in the hypothesis while setting the initial
searched copy count to one. Rank `ASU = nA` states with the versioned empirical
resolution/copy prior, build the dynamic resource plan from the one-copy
workload, and advance the relevant adapter/cache identity.

Focused acceptance cases must prove:

- expected copy counts greater than four remain eligible;
- expected copy counts greater than 16 remain retained without allowing rare
  high-copy states to dominate merely because one solvent-density point lies
  near the mode;
- the searched copy count is one for every initial hypothesis;
- each frozen crystal emits 25 hypotheses from the checksum-frozen audit
  inputs;
- all 75 task identities are unique and stable under input permutation;
- resource plans remain separate from scientific hypothesis identity;
- a resource-plan mutation changes only execution/cache evidence; and
- no active source or configuration contains the retired four-copy/joint-copy
  admission policy.

### 3. Preserve child evidence on failure

Extend strict failed-run collection so every submitted MR child has one bounded,
checksum-authenticated diagnostic record even if the controller terminates
before the normal success inventory is published.

The record must include, when available:

- crystal, hypothesis, model, sequence group, expected copies, and attempted
  copy number;
- native scheduler ID and terminal scheduler/exit state;
- allocated CPUs, memory, time, attempt, elapsed time, and observed resources;
- command, Phaser log, normalised result, solution assets, and checksums; and
- an explicit distinction between completed scientific evidence, killed work,
  absent output, and unsubmitted work.

Collection must be read-only, bounded, path-confined, content-inventoried, and
incapable of making a failed cache reusable.

### 4. Qualify one exact source

Run focused tests first, followed by the named locked integration gate. The
exact pushed commit must pass CI. Deploy only that pushed source and its reviewed
wrapper scripts. Build a fresh source-bound private execution authority whenever
scientific source semantics change.

Required evidence:

- formatting, lint, type, docs, schema, package, and diff hygiene;
- focused ranking, resource, retry, collection, and non-stub Nextflow tests;
- the complete local integration/release gate;
- exact commit/tree and green CI run/job; and
- deployed-tool hashes and strict Phenix/runtime bindings.

### 5. Run fresh discovery and pass-1 screening

Stage and submit one owned discovery run, then one parent-bound screen from the
same exact source. Never use failed, cancelled, predecessor-scientific, or
cross-source caches.

Screen acceptance requires:

- all 75 independent one-copy hypotheses emitted without executor throttling;
- complete per-crystal candidate, task, outcome, resource, retry, and cache
  inventories;
- exact provider/model/input/runtime provenance;
- first and resume traces with justified cache reuse;
- checksum-reconstructible outputs; and
- no reinterpretation of execution failure as scientific no-hit.

### 6. Stop for A-seed review

On successful collection, publish one owned A review package per proceeding
crystal and stop. Each package must retain every candidate, result, asset,
advisory, and rejection reason. No decision may be inferred from rank.

The user must explicitly approve, reject, or defer each candidate target.

### 7. Complete approved copies and finalists

For each approved seed:

1. place additional copies sequentially;
2. retain the best supported copy state and every failed/unsupported addition;
3. refine only the bounded retained finalists;
4. validate Free-R identity and preservation;
5. retain coordinates, maps, raw logs, Rwork/Rfree, geometry and warnings; and
6. compare leading candidates and credible alternatives on the same evidence
   basis.

### 8. Resolve sequence support and report candidates

Score map-supported sequence evidence against the complete trusted catalogue.
Preserve equivalent sequences and compatible loci rather than selecting one by
annotation preference. Stop for mandatory sequence review.

For each crystal, publish a private supervisor-ready report containing:

- leading candidate and complete retained alternatives;
- gene/protein identifiers and coordinate provenance;
- sequence identity/coverage and model limitations;
- expected versus supported copy count;
- LLG, TFZ, packing, refinement, map and sequence evidence;
- negative evidence and explicit rejection reasons;
- confidence class with stated limitations; and
- exact source, tool, database, input, decision and checksum provenance.

### Candidate-roadmap completion condition

This first objective is complete only when every frozen crystal has one
checksum-reconstructible terminal candidate assessment and all required A and
sequence decisions are recorded. An honest unresolved endpoint counts as
completion; a guessed identity does not.

## Remaining v0.3 development after candidate assessment

### M6 operational and leakage qualification

- qualify the operational track on the exact release line;
- produce an observed checksum-bound cache-mutation record;
- run the separately owned leakage track from the accepted operational parent;
- verify truth isolation, task/cache inventories, resources and outputs; and
- close the cancellation and failed-collection defects.

### Complete finding closure

Finish `RG0` through `RG7`. Every original and new finding must have a final
`Fixed`, `Superseded`, or `Deleted` disposition backed by its regression,
exact-source CI, and required fixed-HPC evidence. Unknown pass 2 must reject an
absent, stale, changed, or incomplete closure package.

### Unknown-data second-pass rank recovery

- Freeze the three existing dataset identities and supervisor-confirmed
  candidate identities before execution; keep the latter evaluation-side only.
  All catalogue proteins remain eligible under the declared policy, including
  the confirmed candidates; only expected-answer labels are hidden from ranking.
- Qualify a fixed second-pass route that uses the current production candidate
  ranking and declares its input/candidate/model inventory before execution.
  The existing composition-oriented `unknown-pass2` profile does not implement
  this route and must not be launched as a substitute.
- Run the ranking test on the exact qualified source, preserve complete outputs,
  task/tool/input identities and checksums, then compare with the frozen expected
  candidates. Historical rankings or a cached report from the old source are not
  fresh second-pass evidence.
- Pass only when all three supervisor-confirmed candidates rank first again
  with complete, valid execution evidence. Mark the test complete and continue
  without a further human approval when this condition passes.
- On a different/missing top candidate, incomplete execution or an inconclusive
  comparison, preserve diagnostics and pause for the user. Do not rerank, retune
  thresholds or weaken the pass condition to force recovery.
- Report this as retrospective recovery on previously inspected data, not an
  independent estimate of general accuracy or new map/sequence identification.

Unknown-crystal A continuation, B-F composition exploration, new copy/sequence
claims and their human decisions are deferred to v0.4. Existing known-control
requirements, including 9ECN depth-three qualification, remain separate release
evidence; deeper support remains provisional.

### Final reports, atlas and package surface

- complete per-crystal and cross-crystal reports;
- keep private scientific reports outside the public atlas;
- expose only `genome-to-diffraction` from the installed research package;
- keep the HPC client as an internal Pixi/zipapp validation tool;
- finish canonical module/file naming and remove operational milestone names
  from active executable code;
- make current contracts canonical and isolate release-rotated history; and
- generate deterministic offline HTML and frozen release snapshots from tracked
  sources.

### v0.3.0 release condition

Release only when:

- the three accepted candidate assessments and cancelled investigation retain
  their honest final dispositions, and all three second-pass rank-recovery
  cases pass the criterion above;
- the existing operator decisions are recorded; successful rank recovery
  requires no additional approval and no new unknown-continuation decisions;
- M6 operational, mutation, and leakage evidence passes;
- all findings have final dispositions;
- 9ECN accepts depth three and deeper support remains provisional;
- package, schema, documentation, atlas and install parity passes;
- the exact release commit passes full local, CI, and fixed-HPC gates; and
- the release tag, notes, evidence indexes, and snapshots are immutable and
  mutually checksum-consistent.

## Operational rules

- The active pointer is the sole mutable owner of integrated-run state.
- Never infer ownership from recency or select another run automatically.
- Use the reviewed wrapper only; no raw SSH or direct scheduler commands.
- Issue one shell command per tool call.
- Never submit a duplicate job or reuse a failed/cancelled scientific cache.
- Preserve user work and private data.
- Do not journal unchanged monitoring state.
- Do not fabricate human approval.
- Pause on failed/inconclusive second-pass rank recovery and at any genuinely
  required new-authority boundary. Unknown-continuation review gates remain
  mandatory for their v0.4 scope, not a new v0.3 prerequisite.
