process.executor = 'local'
cleanup = false

trace {
    enabled = true
    file = "${params.outdir}/trace.tsv"
    overwrite = true
    fields = 'hash,process,tag,status,exit,cpus,memory,time,attempt,workdir'
}

report.enabled = false
timeline.enabled = false
dag.enabled = false
